import React from 'react'

const Error = ({ message }) => {
    return (
        <div className='flex flex-1 justify-center items-center'>
            {message}
        </div>
    )
}

export default Error
